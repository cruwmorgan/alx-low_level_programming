#include <stdio.h>

/**
* add - function that adds two integers and returns the result
* Return: result of the addition
* @m: an input
* @n: another input
*/

int add(int m, int n)
{
	int p = m + n;
	return (p);
}
