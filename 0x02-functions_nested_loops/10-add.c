#include <stdio.h>

/**
* add - function that adds two integers and returns the result
* Return: result of the addition
* @m: an input
* @g: another input
*/

int add(int m, int g)
{
	return (m + g);
}
