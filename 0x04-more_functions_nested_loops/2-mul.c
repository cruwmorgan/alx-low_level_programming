#include "main.h"

/**
* mul - function that multiplies two integers
* Return - result of multiplication is returned
* @a: an input
* @b: an input
*/

int mul(int a, int b)
{
	int p = a * b;

	return (p);
}
