#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* main - program that prints the alphabet in lowercase
* 
* Return: 0 is returned
*/

int main(void)
{
	int m;
	for (m = 'a'; m <= 'z'; m++)
		putchar(m);
		putchar ('\n') ;
		m++;
}
